# C3.4 — Scenario Computation v4  

(Сценарные вычисления в архитектуре NeuroTransAnalytics)

## 1. Назначение и место C3.4 в архитектуре v4

Слой **C3.4** является центральным вычислительным ядром проекта NeuroTransAnalytics v4. В этом слое абстрактные компоненты времени (ΔV) и параметры стимулов собираются в конкретные аналитические структуры (сценарии).

Здесь происходит:

- формирование аналитических осей и контрастов;
- расчет метрик для сценариев A0, A1, A2, B1, B2, B3;
- создание стандартизированных вычислительных пакетов (ScenarioResult).

C3.4 работает исключительно с данными, прошедшими через слои C3.1–C3.3.

---

## 2. Методологическая позиция слоя C3.4

Сценарные вычисления — это **процесс алгоритмической сборки результата**.

Слой C3.4:

- не интерпретирует полученные цифры;
- не делает выводов о "норме" или "патологии";
- реализует логику сравнения, описанную в методологических картах уровней A и B.

Конечный продукт C3.4 — это набор чисел и матриц, готовых для технической визуализации (C3.5) или смыслового описания в отчёте (C4).

---

## 3. Входной контракт C3.4

### 3.1 Источники

Данные поступают из:

- **C3.1/3.2:** Векторы RT и компонент ΔV.
- **C3.3:** QC-статусы и базовые агрегаты.

Прямой доступ к `neuro_data.db` из этого слоя запрещен для обеспечения чистоты пайплайна.

### 3.2 Аналитические оси

Для расчетов используются следующие параметры дизайна из SQLite:

- `psi_pre_ms` (предстимульный интервал);
- `stimulus_index` (позиция);
- `stimulus_color` / `stimulus_location` (атрибуты стимула).

---

## 4. Алгоритм исполнения сценария

Каждый сценарий (напр. A1.x "Расхождение архитектур") выполняется по единой схеме:

1. **Выборка:** Загрузка данных по заданному фильтру (напр. "только Tst1 и Tst2").
2. **Валидация:** Отсечение записей с негативным QC-флагом.
3. **Контрастирование:** Расчет разностей или отношений между условиями (напр. RT_{Tst2} - RT_{Tst1}).
4. **Метризация:** Расчет групповых или индивидуальных показателей (медианы контрастов, коэффициенты вариации).
5. **Экспорт:** Упаковка результатов в артефакт `ScenarioResult`.

---

## 5. Стандартные метрики сценариев

### 5.1 Архитектурные метрики (уровни A0–A2)

- Индивидуальные значения ΔV1, ΔV4, ΔV5/MT.
- Доля вклада каждой компоненты в общее время реакции.
- Стабильность компонентной структуры при повторных измерениях.

### 5.2 Контрастные метрики (уровни B1–B3)

- Величины эффектов (Effect Sizes) условий (PSI, Цвет, Позиция).
- Детерминированные разности Δ(ΔV) по осям нагрузки.

---

## 6. Результаты (Output Artifacts)

Слой C3.4 генерирует следующие типы выходных данных:

- **ScenarioResult:** JSON/табличный файл с набором рассчитанных метрик, версией алгоритма и ссылкой на исходные данные.
- **ContrastPack:** Матрица сравнений, необходимая для построения архитектурных карт Divergence/Reallocation.
- **DistributionPack:** Данные для гистограмм и профилей распределения.

---

## 7. Принципиальные ограничения

1. Запрещено вводить в расчеты "экспертные поправки" или веса, не зафиксированные в методологии C1.
2. Каждый расчет должен быть воспроизводим: при подаче того же `SessionID` результат обязан быть идентичным.
3. Сценарные артефакты не должны содержать визуальных настроек (цветов графиков и т.д.) — это задача C3.5.

---

## 8. Передача данных

Результаты сценарных расчетов передаются:
👉 В слой **C3.5 (Visualization)** для построения графических моделей.
👉 В уровень **C4 (Interpretation)** для формирования текстовых заключений.
