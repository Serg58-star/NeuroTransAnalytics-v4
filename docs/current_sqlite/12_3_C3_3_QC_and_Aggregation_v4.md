# C3.3 — Контроль качества и агрегирование данных  

(QC & Aggregation v4)

## 1. Назначение документа

Настоящий документ описывает слой **C3.3** архитектуры NeuroTransAnalytics v4, предназначенный для:

- алгоритмического контроля качества записей (QC);
- детерминированного агрегирования компонент;
- формирования выборок для сценарных вычислений (C3.4).

C3.3 обеспечивает **вычислительную чистоту** пайплайна, гарантируя, что в анализ попадают только данные, соответствующие техническим критериям.

---

## 2. Методологическая позиция C3.3

В версии v4 контроль качества является **автоматизированным фильтром**, а не экспертной оценкой.

Слой C3.3:

- не интерпретирует причины возникновения "плохих" данных;
- не исправляет и не изменяет значения в `neuro_data.db`;
- выполняет маркировку (флагирование) данных на основе жестких алгоритмических правил.

QC и агрегирование — это **технические операции**, подготавливающие данные к сценарному моделированию.

---

## 3. Входные данные слоя C3.3

Входом для C3.3 являются результаты предыдущих этапов:

- событийные данные из C3.1;
- рассчитанные компоненты ΔV1, ΔV4, ΔV5/MT из C3.2.

Все вычисления проводятся с использованием метаданных о дизайне теста, извлекаемых из SQLite.

---

## 4. Контроль качества данных (QC)

### 4.1 Принцип маркировки

Контроль качества в v4 не удаляет записи, а присваивает каждой реакции статус допустимости. Это позволяет сохранять полную прослеживаемость пайплайна.

### 4.2 Алгоритмические уровни QC

#### 4.2.1 Технический QC

Проверка физической целостности записи:

- Наличие RT в таблице `trials`.
- Соответствие типов данных.
- Наличие связанных метаданных в `metadata_*`.

#### 4.2.2 Компонентный QC (Consistency Check)

Проверка логической непротиворечивости результатов C3.2:

- Условие `ΔV* < RT`.
- Соответствие значений ΔV1 физиологически возможным диапазонам (задаются как константы в архитектуре).
- Проверка на отсутствие экстремальных выбросов (напр. RT < 50мс).

#### 4.2.3 Контекстный QC

Проверка соответствия условиям эксперимента:

- Соответствие PSI и индекса стимула структуре сессии.
- Валидность сегмента теста (например, исключение разминочных стимулов из зачетных агрегатов).

---

## 5. Политика фильтрации

Фильтрация данных в v4 выполняется **на лету** при передаче в слой C3.4.

- QC-флаги определяют, включается ли запись в расчет конкретной метрики.
- Исходный массив данных в памяти или БД остается неизменным.

---

## 6. Агрегирование данных

### 6.1 Статус агрегатов

Агрегаты в C3.3 (медианы, MAD, SD) являются промежуточными вычислительными артефактами. Они не считаются финальными результатами анализа, а служат входами для контрастных сравнений в сценариях.

### 6.2 Допустимые алгоритмы

Используются только устойчивые (robust) статистические показатели:

- **Центральная тенденция:** Медиана (как основной показатель).
- **Вариабельность:** MAD (Median Absolute Deviation) и IQR.
- **Архитектурные разности:** Разности медиан по условиям (например, ΔV4_{red} - ΔV1_{baseline}).

### 6.3 Ограничения

Запрещено выполнять агрегирование, скрывающее исходную вариабельность или обходящее QC-флаги. Любой агрегат должен быть трассируем до списка конкретных ID реакций из `neuro_data.db`.

---

## 7. Следующие шаги

Промаркированные и агрегированные данные передаются в ядро сценарных вычислений:
👉 [12_4_C3_4_Scenario_Computation_v4.md](file:///c:/NeuroTransAnalytics-v4/docs/current_sqlite/12_4_C3_4_Scenario_Computation_v4.md)
