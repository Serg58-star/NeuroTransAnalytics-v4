# C3.1 — ETL-пайплайн для модели данных v4

## 1. Назначение документа

Настоящий документ описывает **ETL-пайплайн (Extract–Transform–Load)** в архитектуре NeuroTransAnalytics версии v4.

В версии v4 ETL-пайплайн:

- работает **исключительно поверх базы данных `neuro_data.db`**;
- не занимается первичной миграцией legacy-данных (эта задача решена при создании БД);
- преобразует физические таблицы SQLite в логическую событийную модель для слоёв C3.2–C3.4.

Документ фиксирует **механический и детерминированный характер** извлечения и трансформации данных.

---

## 2. Методологическая позиция ETL в v4

ETL-пайплайн v4 является **инженерным слоем**, обеспечивающим:

- извлечение данных из реляционных таблиц;
- нормализацию временных меток и сопоставление с метаданными стимулов;
- формирование унифицированных структур данных (Events), готовых для компонентного расчёта.

ETL **не выполняет** аналитических расчётов, статистической обработки или интерпретации. Любое преобразование на этом этапе является чисто структурным.

---

## 3. Источники данных (Extract)

### 3.1 Основной источник (SQLite)

Единственным источником данных для ETL является база `neuro_data.db`.

**Ключевые таблицы:**

- `trials` — первичные реакции (RT) и параметры сессий;
- `users` — атрибуты субъектов (пол, возраст);
- `metadata_simple` / `metadata_color_red` / `metadata_shift` — фиксированные параметры стимулов и PSI для соответствующих тестов.

### 3.2 Legacy-контекст

Исторические источники (Excel, Access) в ETL-контуре v4 **не участвуют**. Их структура и формат учитываются только как справочная информация для понимания происхождения данных в `neuro_data.db`.

---

## 4. Процесс трансформации (Transform)

Трансформация в v4 является **строго алгоритмической**:

1. **Чтение записи:** Извлечение RT из таблицы `trials`.
2. **Маппинг стимула:** Связывание индекса реакции (1...36) с метаданными из соответствующей таблицы `metadata_*`.
3. **Восстановление контекста:** Привязка параметров PSI, цвета и позиции к каждой конкретной реакции.
4. **Нормализация:** Приведение типов (например, RT к float) для обеспечения точности последующих расчётов без изменения исходного значения.

---

## 5. Выходные данные (Load)

Результатом работы ETL является формирование в оперативной памяти (или во временных структурах данных) объектов:

- **StimulusEvent:** Параметры предъявленного стимула (цвет, позиция, PSI).
- **ResponseEvent:** Параметры реакции субъекта (RT, статус ошибки).

Эти данные передаются "как есть" на вход слою **C3.2 (Component Timing)**.

---

## 6. Контроль целостности в ETL

На этапе ETL выполняется автоматическая проверка:

- соответствия количества реакций (36 на тест);
- наличия всех необходимых метаданных стимула;
- отсутствия критических разрывов в последовательности сессий.

Любое отклонение фиксируется как технический флаг QC, который передаётся далее по пайплайну.

---

## 7. Следующие шаги

Сформированные события поступают на вход алгоритмам расчёта компонент:
👉 [12_2_C3_2_Component_Timing_Computation_v4.md](file:///c:/NeuroTransAnalytics-v4/docs/current_sqlite/12_2_C3_2_Component_Timing_Computation_v4.md)
