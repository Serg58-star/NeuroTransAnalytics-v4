Уровень C3 — Вычислительные пайплайны и обработка данных (Computation & Pipelines v4)
1. Назначение уровня C3

Уровень C3 описывает:

как данные переходят от физического хранения (C2)

к аналитическим результатам сценариев A0–B3

при строгой воспроизводимости и версионности.

C3 отвечает на вопрос:

каким образом считаются показатели,
и как гарантируется, что любой результат можно пересобрать заново.

C3:

не вводит новых методик (это C1);

не вводит новых сущностей данных (это C2);

реализует вычислительную дисциплину проекта.

2. Принципы вычислительной архитектуры v4
2.1. Принцип пошаговой трассируемости

Каждый вычислительный шаг:

имеет имя;

имеет версию;

сохраняет результат;

указывает входные данные.

Никаких «магических функций».

2.2. Принцип идемпотентности

Повторный запуск пайплайна:

не портит данные;

не перезаписывает предыдущие версии;

создаёт новую версию результата при изменении параметров.

2.3. Принцип сценарной независимости

каждый сценарий (A0.x, A1.x, Bx…)
исполняется как отдельный вычислительный граф;

сценарии не зависят от порядка запуска;

допускается параллельное развитие сценариев.

3. Общая структура C3

C3 организован как каскад пайплайнов, а не как один монолит:

C3.1  ETL → нормализованные события
C3.2  Расчёт компонент (ΔV*)
C3.3  Агрегации и QC
C3.4  Сценарные вычисления (A, B)
C3.5  Визуализационные слои

4. C3.1 — ETL в модель v4
Назначение

извлечь исторические данные;

привести к модели C2.1;

зафиксировать все преобразования.

Ключевые шаги

импорт из Access / CSV / старых форматов;

нормализация времён;

восстановление PSI_test / PSI_mask / PSI_transition;

формирование StimulusEvent и ResponseEvent.

Контроль качества

проверка количества событий;

выявление пропусков;

логирование несоответствий.

5. C3.2 — Расчёт компонент времени (ΔV1, ΔV4, ΔV5/MT)
Назначение

получить компонентную декомпозицию RT;

сохранить версии алгоритмов.

Особенности

ΔV1 — базовая сенсомоторная компонента;

ΔV4 / ΔV5/MT — архитектурные производные;

поддержка нескольких алгоритмов расчёта;

сохранение параметров фильтрации (MAD и др.).

6. C3.3 — Контроль качества и агрегаты
QC

проверка устойчивости компонент;

контроль выбросов;

выявление артефактов дизайна.

Агрегаты

только поверх валидных событий;

с указанием области агрегации;

без потери исходных данных.

7. C3.4 — Сценарные вычисления (A и B)
Структура

Каждый сценарий:

получает входные данные;

применяет фильтры;

считает метрики;

сохраняет результаты и визуализации.

Примеры

A0.x — базовые архитектурные свойства;

A1.x — расхождения архитектур;

A2.x — перераспределение вкладов;

Bx — влияние условий тестирования.

8. C3.5 — Визуализационные пайплайны
Назначение

отделить расчёт от визуализации;

сохранить визуальные артефакты.

Типы

временные профили;

распределения;

архитектурные карты;

популяционные структуры.

9. Управление версиями и воспроизводимость

Для каждого результата фиксируются:

версия данных;

версия алгоритма;

версия сценария;

дата и параметры запуска.

10. Связь с другими уровнями

реализует C1 (методика);

использует C2 (данные);

подготавливает C4 (интерпретация и отчёты).

11. Следующий шаг

После утверждения карты C3 логично:

👉 начать с C3.1_ETL_Pipeline_v4.md
(самый критичный и самый «необратимый» этап).