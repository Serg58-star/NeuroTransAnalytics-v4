# Task 26 — Глобальный интеллектуальный аудит проекта NeuroTransAnalytics-v4

## 1. Цель аудита

Обеспечить полную архитектурную согласованность, чистоту кода и корректность данных в проекте после серии расширений (Task 25.x). Проект должен соответствовать статусу `v4.0.3` (Spatially-Aware & Purified).

## 2. Области проверки

### 2.1 Архитектурные границы (C3 vs C3.5)

- [ ] Проверка отсутствия расчетной логики в `src/gui`.
- [ ] Проверка изоляции через `ScenarioLoader` и `Controller`.
- [ ] Аудит импортов: запрещено импортировать `PySide6` в вычислительные модули (`src/c3_core`).

### 2.2 Чистота данных (Artifact Audit)

- [ ] Аудит всех `.parquet` файлов в `data/derived/scenarios/`.
- [ ] Подтверждение отсутствия `scenario_code` и сервис-версий.
- [ ] Проверка использования `fastparquet` как унифицированного движка.
- [ ] Проверка пространственного разделения (Triad Integrity) в A0.0–A0.3.

### 2.3 Качество кода и Refactoring

- [ ] Удаление "мертвого кода" и закомментированных блоков из `scenario_v4.py`.
- [ ] Синхронизация версий в заголовках файлов и GUI footer.
- [ ] Проверка `PIPELINE_VERSIONS` на актуальность всех компонентов.

### 2.4 Документация

- [ ] Актуализация основных `README` (если требуется).
- [ ] Проверка полноты `walkthrough.md`.

## 3. Критерии успеха (Success Criteria)

1. Код не содержит закомментированных "legacy" фрагментов вычислений.
2. Parquet-файлы содержат только целевые данные без метаданных.
3. GUI отображает версию `scenario_v4.0.3` во всех сценариях блока A0.
4. Выявлены и устранены любые нарушения границ слоёв.

---
Статус: Спецификация готова к выполнению.
