# Interim Summary — Data Adapter v4 (Stage v0)

## 1. Статус пайплайна

Data-adapter v0 полностью работоспособен и проходит полный прогон без ошибок.

Фактический результат последнего запуска:
- Subjects: **1521**
- Sessions: **1902**
- Stimuli: **74178**
- Responses: **205416**
- PSI missing: **0**
- RT validity_flag: **не вычислялся (None)** — ожидаемое нейтральное состояние

---

## 2. Источники данных (зафиксировано)

| Сущность | Источник | Статус |
|--------|---------|--------|
| Subjects | `users.xlsx` | стабильно |
| Sessions / Responses | `boxbase.xlsx` (`cnt` = session_id) | стабильно |
| Warmup | `stimulus_warmup.xlsx` | **единственный источник** |
| Test PSI | `test_metadata_old.py` | агрегировано, стабильно |

**Инвариант:**
PSI нигде не вычисляется в runtime — используется только чтение заранее зафиксированных значений.

---

## 3. Архитектурные инварианты v4

1. `cnt` — единственный идентификатор тестовой сессии во всех слоях данных.
2. Модели `Session`, `StimulusEvent`, `ResponseEvent` согласованы по `session_id`.
3. Warmup-логика:
   - ровно 3 независимых предтестовых блока (`order = 1, 2, 3`);
   - PSI не накапливается между блоками;
   - warmup-контекст (цвет, позиция, test_triple) фиксируется явно.
4. `ResponseEvent.validity_flag`:
   - поле присутствует в модели;
   - значения ещё не вычислялись и равны `None`.

### Уточнение порога валидации RT

Минимально допустимое время реакции зафиксировано на уровне **135 мс**.

Данное значение соответствует параметру `MinRedLight`, изменённому
в программе тестирования **25.12.2016** и отражённому в `config_old.ini`:

MinRedLight = 135


Тестирующая программа не регистрировала реакции быстрее указанного порога,
поэтому использование значения **135 мс** в аналитическом валидаторе
обеспечивает **полную изоморфность** между legacy-ПО и пайплайном анализа данных.

При установленном пороге все реакции в текущем датасете признаны валидными:

RT validity summary: Counter({'OK': 205416})

Использование более жёсткого порога (например, 150 мс) приводит к
искусственному появлению «брака», отсутствующего в исходных данных,
и не рекомендуется.

---

## 4. Сознательно не выполнено (и это корректно)

- не реализована валидация RT;
- не выполнялся анализ влияния warmup на RT;
- не выполнялось объединение событий в аналитические DataFrame.

Эти шаги отнесены к следующему этапу разработки.

---

## 5. Точка отсечки

Все вопросы, связанные с:
- загрузкой legacy-данных;
- нормализацией идентификаторов;
- PSI;
- warmup-контекстом;

считаются **закрытыми и корректно реализованными**.

Возврат к этим вопросам в рамках v4 не предполагается.

---

## 6. Следующий логический шаг

Реализация слоя **валидации RT** с заполнением `ResponseEvent.validity_flag` как независимого аналитического этапа.

# Interim Summary — Data Adapter v4 (Stage: RT × PSI Ready)

> This section extends the previously fixed interim summary.
> Earlier content remains valid and unchanged.

## Статус
Этап построения и валидации слоя data_adapter v4 завершён.
Все источники данных согласованы, данные приведены в аналитически
корректное и воспроизводимое состояние.

Дата фиксации: <указать дату вручную>

---

## Источники данных

Используются следующие канонические источники:

- `users.xlsx` — данные субъектов
- `boxbase.xlsx` — результаты тестирования (RT)
- `stimulus_warmup.xlsx` — предтестовые (warmup) стимулы
- `test_metadata_old.py` — дизайн тестовых стимулов (через TestMetadataManager)

Никакие PSI не вычисляются — используются только зафиксированные
значения из исходных источников.

---

## Объёмы данных (после загрузки)

- Subjects: **1521**
- Sessions: **1902**
- Stimuli events: **74178**
- Response events: **205416**
- PSI missing: **0**

Все RT прошли валидацию.

---

## Валидация RT

Используемые пороги:

- Минимальный RT: **135 мс**
- Максимальный RT: **3000 мс**

Результат валидации:

# Data Adapter v4 — Interim Summary

## Статус
Этап построения и валидации слоя data_adapter v4 завершён.
Все источники данных согласованы, данные приведены в аналитически
корректное и воспроизводимое состояние.

Дата фиксации: 24/01/2026

---

## Источники данных

Используются следующие канонические источники:

- `users.xlsx` — данные субъектов
- `boxbase.xlsx` — результаты тестирования (RT)
- `stimulus_warmup.xlsx` — предтестовые (warmup) стимулы
- `test_metadata_old.py` — дизайн тестовых стимулов (через TestMetadataManager)

Никакие PSI не вычисляются — используются только зафиксированные
значения из исходных источников.

---

## Объёмы данных (после загрузки)

- Subjects: **1521**
- Sessions: **1902**
- Stimuli events: **74178**
- Response events: **205416**
- PSI missing: **0**

Все RT прошли валидацию.

---

## Валидация RT

Используемые пороги:

- Минимальный RT: **135 мс**
- Максимальный RT: **3000 мс**

Результат валидации:

(session_id, stimulus_index)


Брак отсутствует, что соответствует логике исходной программы тестирования
(порог MinRedLight = 135 мс).

---

## RT × PSI Dataset (Tst1)

Собран канонический датасет RT × PSI для теста **Tst1 (ПЗР)**.

Характеристики:

- Rows: **68472** (1902 × 36)
- stimulus_index: **1–36**, без пропусков
- PSI values: **26 уникальных значений**
- Каждая строка соответствует паре:


Доступные поля:

- `session_id`
- `stimulus_index`
- `psi_ms`
- `rt_ms`
- `stimulus_color`
- `stimulus_location`

Датасет не содержит агрегаций и дубликатов.

---

## Методический статус

- Warmup-дизайн и тестовый дизайн согласованы с фактическими данными.
- `test_metadata_old.py` используется строго через `TestMetadataManager`.
- Data adapter v4 считается **закрытым** и не требует дальнейших изменений.

Следующий этап:
**аналитическая нормализация RT по stimulus_index** и анализ зависимости
RT × PSI × перцептивные факторы.

---

## Инварианты (зафиксированы)

- PSI не вычисляются, а читаются из источников.
- RT анализируются только после валидации.
- data_adapter не используется для аналитических преобразований.


Доступные поля:

- `session_id`
- `stimulus_index`
- `psi_ms`
- `rt_ms`
- `stimulus_color`
- `stimulus_location`

Датасет не содержит агрегаций и дубликатов.

---

## Методический статус

- Warmup-дизайн и тестовый дизайн согласованы с фактическими данными.
- `test_metadata_old.py` используется строго через `TestMetadataManager`.
- Data adapter v4 считается **закрытым** и не требует дальнейших изменений.

Следующий этап:
**аналитическая нормализация RT по stimulus_index** и анализ зависимости
RT × PSI × перцептивные факторы.

---

## Инварианты (зафиксированы)

- PSI не вычисляются, а читаются из источников.
- RT анализируются только после валидации.
- data_adapter не используется для аналитических преобразований.





